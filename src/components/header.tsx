import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <header class="bg-gray-50 py-4">
      <h1 class="mx-auto max-w-7xl text-center font-semibold">
        🍕 My Pizza Calculator
      </h1>
    </header>
  );
});
